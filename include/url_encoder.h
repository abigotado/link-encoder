#ifndef URL_ENCODER_H
#define URL_ENCODER_H

// Encode function for null-terminated string
char* url_encode(const char* input);

// Decode function for null-terminated string
char* url_decode(const char* input);

#endif // URL_ENCODER_H 